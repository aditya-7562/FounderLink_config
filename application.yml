spring:
  data:
    redis:
      host: redis
      port: 6379
  cache:
    type: redis
    redis:
      time-to-live: 600000

management:
  zipkin:
    tracing:
      endpoint: http://zipkin:9411/api/v2/spans